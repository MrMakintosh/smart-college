<h1>Авторизация</h1>
<p>Для просмотра или редактирования информации авторизуйтесь!</p>
<div class="container">
  <input type="checkbox" id="switch">
  <label for="switch">
    <b class="top"></b>
    <b class="bottom"></b>
  </label>
  <div class="login">
    <h4>Вход</h4>
    <%= form_for @user_session do |f| %>
        <p>
          <label for="user">Логин</label>
          <%= f.text_field :login %>
        </p>
        <p>
          <label for="pass">Пароль</label>
          <%= f.password_field :password %>
        </p>
        <p>
          <%= f.submit "Войти" %>
        </p>
    <% end %>
  </div>
</div>